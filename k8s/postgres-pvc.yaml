apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: puku-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: .5Gi
  storageClassName: standard